server.port=${PORT:8080}
spring.datasource.url=${DB_URL:jdbc:h2:file:./data/mastan;AUTO_SERVER=TRUE}
spring.datasource.username=${DB_USERNAME:sa}
spring.datasource.password=${DB_PASSWORD:}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.open-in-view=false
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=30MB
spring.h2.console.enabled=true

app.jwt.secret=${JWT_SECRET:change-this-secret-to-a-long-random-value-at-least-32-characters}
app.jwt.expiration-ms=${JWT_EXPIRATION_MS:86400000}
app.admin.email=${ADMIN_EMAIL:kafeelahmad1314@gmail.com}
app.admin.password=${ADMIN_PASSWORD:Kaf@12345}
app.cors.allowed-origins=${CORS_ALLOWED_ORIGINS:*}
app.upload-dir=${UPLOAD_DIR:./uploads}
